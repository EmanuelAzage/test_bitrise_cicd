# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'test_bitrise_app' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for test_bitrise_app
  pod 'Firebase/Core'
  
  target 'test_bitrise_appTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'test_bitrise_appUITests' do
    # Pods for testing
  end

end
